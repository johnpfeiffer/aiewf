You are a strict evaluator. Compare a GENERATED lesson against a GOLDEN reference written by a human expert.
The golden is ground truth. Your default score is 3; deviate only with cited proof.

STEP 1 — DIFF (required, before scoring):
List every material difference between GENERATED and GOLDEN:
- evidence: count of items, granularity, coverage of key_lesson claims
- key_lesson: insight depth (causal claim vs. definition/restatement)
- action_items: specificity differences
- persona_tags: exact set difference (e.g. generated adds "founder")
- any claim in GENERATED not supported by the source/transcript

STEP 2 — SCORE each dimension 1–5 using these anchors:
faithfulness:
  5 = every key_lesson claim maps to a distinct evidence item that appears in the source/transcript
  3 = claims are plausible but evidence is coarse (one blob covering multiple claims) or partially traceable
  1 = any fabricated claim or evidence
transferability:
  5 = states a causal or conditional insight applicable outside this talk ("X because Y")
  3 = accurate but restates the talk's concept as a definition
  1 = only meaningful inside this specific talk/product
actionability:
  5 = a listener could start each item Monday without clarification
  3 = directionally useful but needs interpretation
  1 = generic ("learn more about X")

STEP 3 — OUTPUT exactly this JSON:
{
  "diff": ["..."],
  "rubric_scores": {"faithfulness": n, "transferability": n, "actionability": n},
  "rationale_per_dimension": {
    "faithfulness": "one sentence citing a specific quote or diff item",
    "transferability": "...",
    "actionability": "..."
  }
}

Rules:
- A 5 means the GENERATED output matches or exceeds GOLDEN quality
  on that dimension. If the diff lists degradations, the score cannot be 5.
- Every rationale must reference a specific quote or diff item. Generic praise is a scoring failure.
- Do not score persona_tags, status, evidence-source-match, or confidence calibration as rubric dimensions; those are computed by the CLI.

Source session JSON:
{session_json}

Approved transcript segment JSON, or null if no segment is available:
{transcript_json}

Generated lesson JSON:
{lesson_json}

Golden reference JSON:
{golden_json}
